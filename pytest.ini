[pytest]
# Add the repo root to sys.path so tests can import the top-level modules
# (risk_manager, security, backtest_engine, ...) without a package install.
pythonpath = .
testpaths = tests
addopts = -ra
